<!Doctype html>
<html>
<head>
  <meta charset="utf-8">
  <title>京葉線の駅</title>
</head>
<body>
  <h2>京葉線の駅の詳細</h2>
  <table>
      <% for( let row of data ) { %>
        ID:<%= row.id %><br>
        駅コード:<%= row.code %><br>
        駅名:<%= row.name %><br>
        乗換:<%= row.change %><br>
        乗員:<%= row.passengers %><br>
        東京からの距離:<%= row.distance %><br>
      <% } %>
  </table>
</body>
</html>